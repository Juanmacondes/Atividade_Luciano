name: Ola Mundo
on: push
jobs:
  Prazer:
    runs-on: ubuntu-latest
    steps:
      - name: Dizer ola
        run: date
      - name: Dizer ola
        run: echo "Commit feito por ${{ github.actor }}"
